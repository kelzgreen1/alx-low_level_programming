#include <stdio.h>
/**
 * main - this code print the alphabet in lower caps
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;

	c = 'a';
	while
	(c <= 'z')
	{
	putchar(c);
	c++;
	}
putchar('\n');
return (0);
}
