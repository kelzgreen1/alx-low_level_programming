#include <stdio.h>
/**
 * main - this code prints all the single digit no of base 10
 * Return: Always 0 (Success)
 */

int main(void)
{
	int p;

	for (p = 0; p < 10; p++)
		printf("%d", p);
	putchar('\n');
	return (0);
}
