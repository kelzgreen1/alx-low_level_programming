#include "main.h"
/**
 * _abs - compute the absolute valie of an integer
 * @n: the integer to be computed
 * Return: the absolute value of the integer
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
